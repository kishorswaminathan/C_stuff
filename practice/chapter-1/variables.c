#include<stdio.h>

int main()
{
    int a = 4;
    // int b = 8.5;  // Not Recommended because 8.5 is not an integer

    float b = 8.5;
    char c = 'u';
    printf("the value of a is %d", a);

        return 0;
}